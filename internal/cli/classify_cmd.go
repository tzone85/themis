package cli

import "github.com/spf13/cobra"

// newClassifyCmd is a stub completed in Plan-2 / T13 (themis classify CLI).
// Kept here so the root command builds.
func newClassifyCmd() *cobra.Command { return &cobra.Command{Use: "classify"} }
